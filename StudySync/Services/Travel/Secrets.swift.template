// Template for Secrets.swift.
//
// SETUP:
//   1. Copy this file to `Secrets.swift` in the same directory:
//        cp Secrets.swift.template Secrets.swift
//   2. Fill in your API keys.
//   3. Build.
//
// `Secrets.swift` is gitignored — don't commit it.
// This template (`Secrets.swift.template`) IS committed, so anyone cloning
// the repo knows which secrets to fill in.
//
// The `.template` extension is intentional: Xcode's file-system-synchronized
// target membership only picks up `.swift` files, so this template file is
// never compiled. If you rename it without removing `.template`, you'll get
// two conflicting `enum Secrets` declarations and a build error.
//
// ─── Contents ───────────────────────────────────────────────────────────

import Foundation

enum Secrets {
    /// AviationStack API key. Free tier: 100 requests/month. Same-day only.
    /// Sign up at https://aviationstack.com/signup/free
    static let aviationStackAPIKey = "PUT_YOUR_KEY_HERE"

    /// AeroDataBox (RapidAPI) key. Free tier: 500 requests/month.
    /// Preferred provider — supports future + historical flight dates.
    /// Sign up at https://rapidapi.com/aedbx-aedbx/api/aerodatabox
    static let aeroDataBoxAPIKey = "PUT_YOUR_KEY_HERE"
}
